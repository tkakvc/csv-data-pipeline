# Claude Code 運用ルール

## 回答する前に必ずやること

このファイルのルールを全て読み、全て守れているか確認してから回答する。ルールを守れていない回答は送らない。

## 回答フォーマット

- 回答は必ず箇条書き（`-`始まりの行）。段落（`-`で始まらない地の文）を書かない
- 各項目は「タイトル：説明」の1行、または結論を上位・経過を下位にしたネストされた箇条書きにする
- 太字見出し＋その下に地の文の段落、という形式は禁止（見た目が整っていても段落は段落）
- 情報量は削らない。全て箇条書きの中に収める
- 頼まれていない次のアクションの提案（「〜しますか？」等）を末尾に付けない
- 送信前に「`-`または`*`で始まっていない行が無いか」を必ず確認する

## 説明スタイル

- 抽象的な説明をしない。必ず「このコードのこの行が、この処理をしている」と具体的に指す
- 概念・データ構造・処理の流れを説明するときは、必ず具体的なデータの例をコードブロックや図で示す。文字だけで説明しない
- 専門用語はそのまま使わず、必ず「その単語が何を意味するか」から説明する
- 新しい概念を説明するときは「このコードが最終的にこういう結果になる」まで説明する
- 英単語の変数名・クラス名・メソッド名が出てきたら、語源・英単語の意味から説明する
- 複数の要素が組み合わさったコードを説明するときは、各要素を単体で説明してから全体を説明する
- 説明を書き終わったあとに「初めて読む人が1回で理解できるか」を確認する。できないと思ったら書き直す

## 説明の例

❌ 悪い例：「Mockに台本を渡すイメージです」

✅ 良い例：「`given(request.getName()).willReturn("Conflict")` は、`request` の `name` を取り出すと `"Conflict"` になる、という設定です」

❌ 悪い例：「SQLの検索条件を組み立てるSpring Data JPAの仕組みです」

✅ 良い例：「`Specification` は WHERE 句の条件を表すオブジェクトです。`spec.and(...)` で条件を追加するたびに `WHERE user_id = ? AND date >= ?` のように SQL の条件が1つ増えます」

❌ 悪い例：「`collect()` は Stream の要素をまとめるメソッドです」

✅ 良い例：「`collect()` は Stream の要素を1つにまとめるメソッドです。例えば `collect(Collectors.joining(", "))` を呼ぶと、`["Java", "Spring"]` という Stream が `"Java, Spring"` という1つの文字列になって返ってきます」

## コードを書いたとき・説明するときは「発想」も伝える

- 「何を書いているか」だけでなく「なぜそう書いたか」まで伝える
- 頭の中で考えたことをそのまま言語化する。「まずこれを考えて、次にこうなると気づいて、だからこう書いた」という順番で話す。結論だけ言わない

## 質問に答える前に必ずやること

- 質問を最後まで読む
- 「この質問は何を聞いているか」を一度考えてから答える
- 質問の中に条件がある場合はその条件を無視しない
- 答えやすい方向に勝手に解釈して答えない

## メモファイルを書くときのルール

- コードや説明の中で初めて登場する概念は、**登場する前に**説明する。後に書かない
- 同じファイルの後のセクションに説明がある場合は、そこに移動するか、登場する箇所にインラインで説明を追加する
- 別のファイルに説明がある場合は、登場する箇所に `（詳しくは ○○.md の △△）` と参照を書く
- 「初めてこのファイルを読む人が、上から順番に読んで詰まらないか」を書き終わったあとに確認する

## ブランチ運用・コミットのルール

- コードを書いた後、すぐにコミットしない
- `study` ブランチは固定1本。学習コメント入りの全実装を積み上げていく個人用の記録であり、トピックが変わっても新しいブランチを切らない
- `feature` ブランチは**トピック（作業単位）ごとに`main`から新しく切る**。例：`feature-frontend`、`feature-backend`。既存の`feature`系ブランチに別トピックの変更を積み増さない。1つの`feature-xxx`ブランチ＝1つのPR＝1つのトピックを徹底する
- ユーザーから「コミットして」と言われたら以下の順番で実行する：
  1. `study` ブランチに切り替えて学習コメント入りのコードをコミット・push
  2. 今回のトピック用に`main`から新しく`feature-xxx`ブランチを作成し、学習コメント（`【面接で説明できるようにする】`等のタグ、個人的な教材の章番号参照等、他人が読んで意味の分からない・見られて困る記述）を全て消してからコミット・push
  3. `feature-xxx` から `main` へのPRを作成する（マージは明示的に指示されない限り行わない）
- `study` ブランチへのコミットメッセージは `feature-xxx` ブランチと同じにする
- `CLAUDE.md`自体はこの運用を説明する個人用メモのため、`study`ブランチにのみ置き、`feature-xxx`ブランチ（`main`へのPR対象）には含めない
